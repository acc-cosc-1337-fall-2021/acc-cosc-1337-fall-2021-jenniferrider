// include
#include "tic_tac_toe_data.h"

void TicTacToeData::save_games(const std::vector<unique_ptr<TicTacToe>>& games)
{
    //Open file for writing
    //For each vector of TicTacToe
    //For each char in string 
    //Write char to file end char in string loop
    //Write get winner of game to file
    //Write a new line
    //end vector of TicTacToe loop    

}

vector<unique_ptr<TicTacToe>> get_games()
{
    //Algorithm to restore a vector of unique ptr of TicTacToe from file.
    //(Each line in the file is a game(vector of string), each character in the line is the , 
    //and from left to right position 0,1, etc.). The last character in each line is the game winner.

}